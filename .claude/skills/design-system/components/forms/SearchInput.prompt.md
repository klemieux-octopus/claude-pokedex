**SearchInput** — rounded search field with blue LCD focus glow. Pass a Lucide search icon as `icon`.

```jsx
<SearchInput placeholder="Search the dex…" icon={<i data-lucide="search"></i>} />
```

Sizes `sm|md|lg`.
