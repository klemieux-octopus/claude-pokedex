**IconButton** — square or round control for a single glyph (nav, close, favourite). Pass a Lucide SVG as children; always set `label`.

```jsx
<IconButton label="Search" variant="secondary"><i data-lucide="search"></i></IconButton>
```

Variants match Button. `round` for circular. Sizes `sm|md|lg`.
