**TypeBadge** — elemental type chip. Solid type-colored capsule with white uppercase label. Use everywhere a Pokémon's type appears.

```jsx
<TypeBadge type="fire" /> <TypeBadge type="flying" size="sm" />
```

18 types supported (normal…fairy). Sizes `sm|md|lg`.
