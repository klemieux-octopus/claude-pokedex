**Badge** — small status/count pill for metadata (region, generation, counts). Distinct from TypeBadge.

```jsx
<Badge tone="accent">Gen I</Badge> <Badge tone="success">Caught</Badge>
```

Tones: `neutral | brand | accent | success`.
