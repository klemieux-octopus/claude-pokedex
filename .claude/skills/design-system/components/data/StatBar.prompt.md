**StatBar** — labelled base-stat meter with retro inset track. Fill auto-colors by value (green/yellow/red) unless overridden.

```jsx
<StatBar label="HP" value={78} />
<StatBar label="ATK" value={130} color="var(--type-fire)" />
```
