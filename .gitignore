# We work exclusively with Claude Code, which discovers skills in .claude/skills/.
# The vendor-neutral .agents/skills/ copy is redundant and not tracked here.
.agents/

# App dependencies / build output (the app lives at the repo root)
node_modules
dist
coverage
*.local
.vite
*.tsbuildinfo

# Throwaway design mockups
prototype/

# Generated PokéAPI snapshot (rebuilt at build time / in CI, not committed)
public/data/

# OS cruft
.DS_Store
Thumbs.db

# Editor
.idea/
*.swp

# Logs
*.log
npm-debug.log*
